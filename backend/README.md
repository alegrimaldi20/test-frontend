# Litebox Backend API

## Setup
npm install
npm run start:dev

## Endpoints
- GET  /api/posts/related     → list related posts
- POST /api/post/related      → create post (multipart: title + image)

## Production
npm run build
npm run start
